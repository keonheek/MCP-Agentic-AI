A decision or pattern just happened. Log it immediately while the context is hot.

Look at the current conversation and identify what was just decided or learned. Then:

**If it's a decision** (pivot, new direction, dropped priority, architectural choice, tradeoff made):
Append to `decisions/log.md`:
`[YYYY-MM-DD] DECISION: {what was decided} | REASONING: {why} | CONTEXT: {one sentence of session context}`

**If it's a correction or stable pattern** (something Claude got wrong, a preference confirmed, a workflow rule):
Write or update the relevant file in `C:\Users\keonh\.claude\projects\c--Users-keonh-Dev-MCP-Agentic-AI\memory\` and update `MEMORY.md` index.

**If it's both**, do both.

Show a one-line confirmation of what was written. Nothing else.
