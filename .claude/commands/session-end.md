Read `tasks/todo.md`. Scan the current session context.

Do the following, silently, no confirmation:

**1. Update `tasks/todo.md`:**
Mark completed items done. Add new blockers discovered this session. Remove stale items that are no longer relevant.

**2. Append one-line entry to `templates/session-log.md`:**
Format: `[YYYY-MM-DD] [one sentence: what the session focused on] | Built: [X or none] | Blocked: [Y or none]`
If the file does not exist, create it with a header `# Session Log` before appending.

That's it. Do not write to decisions/log.md or memory — use /mark during the session for those.
